# =============================================================================
# Vite & Gourmand — Configuration TESTS
# =============================================================================
# Copier ce fichier : cp .env.test.example .env.test
# Les valeurs par défaut correspondent aux containers Docker de test.
# =============================================================================

# APP
APP_ENV=test
APP_DEBUG=false
ENV=test
DEBUG=false

# MySQL (TEST) — port 3307 mappé sur le host depuis mysql-test
DB_HOST=127.0.0.1
DB_PORT=3307
DB_NAME=vite_gourmand_test
DB_USER=root
DB_PASSWORD=rootpass
DB_PASS=rootpass

# MongoDB (TEST) — port 27018 mappé sur le host depuis mongodb-test
MONGO_HOST=127.0.0.1
MONGO_PORT=27018
MONGO_DB=vite_gourmand_test
MONGO_USERNAME=root
MONGO_PASSWORD=rootpass
MONGO_USER=root
MONGO_PASS=rootpass

# JWT (généré automatiquement par le script de test, pas besoin de le définir)
# JWT_SECRET=

# MAIL (TEST) — placeholders, les emails ne sont pas envoyés en tests
MAIL_HOST=smtp.example.com
MAIL_USERNAME=test
MAIL_PASSWORD=test
MAIL_FROM_ADDRESS=test@vitegourmand.fr
CONTACT_EMAIL=contact-test@vitegourmand.fr
